# This file records generated artifacts and secrets that should never be committed.
# It remains a small dedicated file so repository hygiene is visible at a glance.
# Git references this list whenever it decides what belongs in version history.

node_modules
.next
out
coverage
playwright-report
test-results
.env
.env.local
.DS_Store
*.log
